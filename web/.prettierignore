dist
node_modules
pnpm-lock.yaml

# Generated by the TanStack Router plugin on every build.
src/routeTree.gen.ts

# Markdown follows the repo-wide convention instead (no manual wrapping, tables
# written by hand); formatting only web/'s Markdown would leave the repo with two
# styles.
*.md
